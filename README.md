두 정수를 입력받아 최대공약수(GCD)와 최소공배수(LCM)를 계산하는 프로그램인데 
먼저 최대공약수 계산은 함수 GCD(a, b)를 정의하고
만약 b가 0이면, GCD(a, b)는 a를 반환한다 그리고
그렇지 않으면GCD(a, b)는 GCD(b, a % b)를 호출한다.
최소공배수 계산은 함수 LCM(a, b)를 정의하고
LCM(a, b)는(a * b) / GCD(a, b)를 반환한다.
입력값중 둘중하나가 0이면 오류메세지를 출력한다.
